# Install v2.0 Planner Test
Extract into the repository root, then run:

```powershell
python -m mkdocs build --strict
git add .
git commit -m "Add planner card and finalize Universal options"
git push
```

Test August 10, 13 and 14 on a phone. Verify the Planner card, confirmed breakfasts, optional Hogwarts Express extensions, and clearer timing labels.
